# Dockerfile
FROM alpine:latest          # Use a minimal Linux image
COPY hello.txt /hello.txt   # Copy a simple text file into the image

CMD ["cat", "/hello.txt"]   # When container runs, print the contents of the file
